apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "eoloserver.fullname" . }}-test-connection"
  labels:
    {{- include "eoloserver.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "eoloserver.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
